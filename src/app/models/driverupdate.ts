export class DriverUpdate{
    constructor(public driverId:number, public username:string, public password:string, public mobileNumber:string, public email:string,
        public licenseNo:string, public carType:string, public rating:number){}
}